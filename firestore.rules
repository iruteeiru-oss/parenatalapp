rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Direct-pairing access based on the secure room code
    match /monitors/{parentId}/{document=**} {
      allow read, write: if true;
    }
  }
}